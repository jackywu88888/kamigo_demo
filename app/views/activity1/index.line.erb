[
  <% if @keyword.present? %>
  {
    "type": "text",
    "text": "您要查詢(<%= @keyword %>)的關鍵字題目如下。"
  },
  <% end %>

  <%= raw (Kamiflex.json(self) do
    alt_text "各項活動"
  
    carousel do
      bubbles @activitys1 do |activity|
        body do
        text activity [:名稱], weight: :bold, size: :xl
        baseline_box margin: :md do
        end
        vertical_box margin: :lg, spacing: :sm do
          vertical_box spacing: :sm do
            text "活動說明", color: "#aaaaaa", size: :sm, flex: 1
            text activity [:說明], wrap: true, color: "#666666", size: :sm, flex: 5
          end
          vertical_box spacing: :sm do
            text "時間", color: "#aaaaaa", size: :sm, flex: 1
            text activity[:時間], "wrap": true, color: "#666666", size: :sm, flex: 5
          end
        end
        end
        footer do
          url_button "連結", activity[:連結], style: :primary
        end
      end
    end
  end) %>

]