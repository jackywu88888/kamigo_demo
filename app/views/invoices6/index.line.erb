[
  <% if @keyword.present? %>
  {
    "type": "text",
    "text": "您要查詢(<%= @keyword %>)的關鍵字題目如下。"
  },
  <% end %>


  <%=raw(Kamiflex.json(self) do
    alt_text "發票載具設定"
  
    carousel do
      bubbles @invoices6 do |invoice|
        hero invoice [:圖片顯示], size: :full, aspectRatio: "390:500", aspectMode: :cover, action: uri_action("https://www.water.gov.tw/einvoice/CloudInvoice/Transfer?nodeId=4622")
        body spacing: :sm do
          text invoice [:發票商家], wrap: true, weight: :bold, size: :md, align: :center, color: "#F00078"
          vertical_box height:"100px" do
          text invoice [:步驟教學], wrap: true, weight: :bold, size: :sm, color: "#000dff"
          end
          end
        footer do
          url_button "圖片查看", invoice [:圖片顯示], style: :primary
        end
      end
    end
  end) %>
  

]