[
  <% if @keyword.present? %>
  {
    "type": "text",
    "text": "您要查詢(<%= @keyword %>)的關鍵字題目如下。"
  },
  <% end %>

  <%= raw(Kamiflex.json(self) do
    alt_text "LINE Flex Message Showcase: Shopping"
    carousel do
      bubbles @momos3 do |momo|
        hero momo[:圖片], size: :full, aspectRatio: "1820:1024", aspectMode: :cover
        body spacing: :md ,layout: "vertical"do
          text momo[:活動], wrap: true, weight: :bold, size: :xl, align: "center", color: "#ff5551"
          end
        end
        footer do
          url_button momo[:連結名稱], momo[:連結], style: :primary, color: "#ff5551"
        end
      end
    end
  end
  
  puts json)%>
]