[
  <% if @keyword.present? %>
  {
    "type": "text",
    "text": "您要查詢(<%= @keyword %>)的關鍵字題目如下。"
  },
  <% end %>

  <%= raw(Kamiflex.json(self) do
    alt_text "LINE Flex Message Showcase: Menu"
    bubble do
      bubbles @momos3, size: :micro do |momo|
      hero momo[:圖片], size: :full, aspectRatio: "1820:1024", aspectMode: :cover, 
        text momo[:活動], wrap: true, weight: "bold",align:"center",size: "md" ,color:"#F00078"
         vertical_box do       
          vertical_box layout: "vertical" , height: "125px",spacing: "xs" do
            text "活動", wrap: true,weight: "bold",size: "lg",flex: 0,color: "#0000C6"
            text momo[:名稱],wrap: true,weight: "bold",size: "sm",flex: 1,color: "#000000",margin: "none"
          end
        end
      end
      footer do
        spacer size: :xxl
        url_button momo[:連結名稱], momo[:連結], style: :primary, color: "#FF0000"
    end
  end
end)%>
]