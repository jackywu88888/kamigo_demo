[
  <% if @keyword.present? %>
  {
    "type": "text",
    "text": "您要查詢(<%= @keyword %>)的關鍵字題目如下。"
  },
  <% end %>

  <%= raw (Kamiflex.json(self) do
    alt_text "MOMO活動查詢"
    carousel do
      bubbles @momos3, size: :micro do |momo|
        hero momo[:圖片], size: :full, aspectMode: "cover", aspectRatio: "1820:1024"
        body spacing: :sm, paddingAll: "13px" do
          vertical_box height:"100px" do
          text momo[:活動], weight: :bold, size: :sm, wrap: true   
          text momo[:名稱], weight: :bold, size: :sm, wrap: true
          end
          url_button momo[:連結名稱], momo[:連結], style: "primary" , heigh:"sm",spacing: "xs"
        end
      end
    end
  end )%>
]